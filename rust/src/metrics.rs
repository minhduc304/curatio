//! Counters + Axum metrics routes — same JSON contract as src/api/app.py. TDD §5, §12.

// TODO: FunnelStats struct (serde, mirrors src/models.py) + GET /stats, /ws/metrics,
//       /health on an axum::Router (TDD §5).
